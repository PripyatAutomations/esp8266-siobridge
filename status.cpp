#include "siobridge.h"
/*
FSInfo fs_info;
SPIFFS.info(fs_info);

struct FSInfo {
    size_t totalBytes;
    size_t usedBytes;
    size_t blockSize;
    size_t pageSize;
    size_t maxOpenFiles;
    size_t maxPathLength;
};
*/
